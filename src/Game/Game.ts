import { type EngineGame, type Graphics } from "../Engine";

export class Game implements EngineGame {
  update(msPerUpdate: number): void {
    // todo
  }

  draw(graphics: Graphics, lag: number): void {
    // todo
  }
}
